# .env.example
# This file is a template for the .env file.
# Copy this file to .env and fill in the actual values. DO NOT commit the .env file.

# --- Django Core Settings ---
# IMPORTANT: Generate a new, strong secret key for production.
# You can generate one using: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=

# Set to 1 for development, 0 for production.
DEBUG=1

# Comma-separated list of hosts for production (e.g., yourdomain.com,www.yourdomain.com)
ALLOWED_HOSTS=localhost,127.0.0.1


# --- PostgreSQL Database Settings ---
# These are the default values used by the local docker-compose.yml.
# For production, you should use strong, unique credentials.
DB_NAME=pilgrims_db
DB_USER=pilgrims_user
DB_PASS=supersecretpassword
DB_HOST=db
DB_PORT=5432